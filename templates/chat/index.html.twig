{% extends 'base.html.twig' %}

{% block title %}Tof'Box{% endblock %}

{% block body %}

<h1 class="center ">Démarrer une conversation entre {{currentUser.nickname }} & {{ user2.nickname }}</h1>

{% for chat in currentUser.chats %}
    {% for user in chat.users %}
        {{dump(user) }}
    {% endfor %}

{% endfor %}


<div class="center">
<form action="{{ path('chat_add_message', {'id':user2.id}) }}", method="POST">
    <p >
        <textarea name="text" id="text" cols="30" rows="10"></textarea>
    </p>
    <p>
        <input type="submit" value="Envoyer">
    </p>

</form>
</div>

{% endblock %}
